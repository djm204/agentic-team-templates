---
description: Project overview and critical requirements - agentic-team-templates CLI, template structure. Reference for understanding project context.
alwaysApply: false
---

# Project Overview

**agentic-team-templates** — CLI that installs AI assistant rules (Cursor, Claude, Copilot) into projects.

## Key Facts

- Node.js CLI; templates in `templates/<category>/<template>/`
- Shared rules in `templates/_shared/` (always installed)
- Each template has `.cursor/rules/` with `.mdc` files
- Registry in `src/index.js` (TEMPLATES object)
- Run: `node bin/cli.js <template> [--ide=cursor|claude|codex]`
